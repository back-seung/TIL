package Main;import java.util.ArrayList;import java.util.List;import java.util.Random;public class oddExceptionExample {    // FIELDS    Random random = null;       // 랜덤    final int LOOP_COUNT = 10;      // 루프 횟수    // 홀수와 짝수를 검증함    public String compare(int compareNum) {        try {            if (compareNum % 2 != 0) {                throw new OddException("THIS IS ODD NUM!");            }        } catch (OddException e) {            System.out.println(e.getMessage());        }        System.out.println("NOW IS LEARNING :D !!!!");        return "THIS IS EVEN NUM!";    }    // 10개의 난수를 생성함    public List<Integer> makeRandomNumbers() {        random = new Random();        List<Integer> RandomNumbers = new ArrayList<>(LOOP_COUNT);        for (int i = 0; i < LOOP_COUNT; i++) {            RandomNumbers.add(random.nextInt(10));        }        return RandomNumbers;    }    // compare를 List의 사이즈만큼 실행    public void execute(List<Integer> randomNumbers) {        for (Integer num : randomNumbers) {            System.out.println("NUMBER : " + num);            compare(num);        }    }    public static void main(String[] args) {        oddExceptionExample o = new oddExceptionExample();        List<Integer> randomList = o.makeRandomNumbers();        o.execute(randomList);    }}